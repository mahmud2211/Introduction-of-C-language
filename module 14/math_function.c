#include<stdio.h>
int main()
{
    // double x;
    // scanf("%lf",&x);
    // int ans=ceil(x);
    // int ans=floor(x);
    // int ans=round(x);
    int x;
    scanf("%d"&x);
    double ans=sqrt(x);
    printf("%0.2lf",ans); 
    return 0;
}